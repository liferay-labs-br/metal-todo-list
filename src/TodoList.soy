{namespace TodoList}

/**
 * This renders the component's whole content.
 */
{template .render}
	{@param newElement: any}

	<div class="todo-list">
		<input onChange="{$newElement}" type="text">
	</div>
{/template}
